name: Build Atomlang In Windows

on: [push, pull_request]

env:
  BUILD_TYPE: Release

jobs:
  bench:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run Build Script
        run: |
          cmd /c bulid.bat
    